# Clinic desk portal on Vercel (the Vercel project's install command reads this).
# Keep in step with [project].dependencies in pyproject.toml.
fastapi>=0.115.0,<1
uvicorn>=0.32.0
livekit-api>=1.2.1,<2
asyncpg>=0.31.0,<1
python-dotenv>=1.0

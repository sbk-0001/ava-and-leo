# Lean install for the Strategybyte Ava desk on Vercel.
# Keep this file off the voice-worker SDK — that stack belongs on LiveKit Cloud.
fastapi>=0.115.0
python-dotenv>=1.0.1
livekit-api>=1.0.0
pydantic>=2.0.0
httpx>=0.27.0
starlette>=0.40.0
uvicorn>=0.32.0
